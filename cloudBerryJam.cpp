#include <bits/stdc++.h>
using namespace std;

int solve(int n)
{
    /*

    25% weight of total weight evaporates after the production
    */

    // Return the amount of berries needed to prepare n 3kg jars of jams
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        cout << solve(n) << endl;
    }
    return 0;
}
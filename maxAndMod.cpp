#include <bits/stdc++.h>
using namespace std;

vector<int> solve(int n)
{
    /*
    max(pi - pi-1) mod i = i-1

    Find the permutation of lenght n
    If no such permutation exists, return -1



    */
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
    }
    return 0;
}
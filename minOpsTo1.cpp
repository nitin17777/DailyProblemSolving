#include<bits/stdc++.h>
using namespace std;

int ops(vector<int>&nums)
{
    //we have to fill the array with 1 only and have to return min operations required to do so // we can flip by taking 3 consecutive elements at once


    int operations = 0;
    
    //base case : when 1st number is 0
    if(nums[0] == 0)
    {

    }
}

int main()
{

    
    return 0;

}
#include <bits/stdc++.h>
using namespace std;

bool can(string s)
{
    int n = s.size();

    /*
     */
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;

        string s;
        cin >> s;

        cout << (can(s) ? "Yes" : "No") << endl;
    }

    return 0;
}
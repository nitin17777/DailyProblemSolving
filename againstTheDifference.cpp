#include<iostream>
#include<vector>
using namespace std;


int solve(vector<int>&arr)
{

    //An array is a block if all elements in array = Length of the array

    int n = arr.length();
    
    //an array is called neat if it can be obtained by concatenation of any number of blocks(possible zero) + An empty array is always neat

    //Find the length of it's longest neat subsequence

    


}

int main()
{
    int n = 2;
    vector<int>arr = {2,2};
    cout<<solve(arr)<<endl;

    return 0;


}
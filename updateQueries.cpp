#include<bits/stdc++.h>
using namespace std;

string solve(string s,string c,vecotr<int>&idx)
{
    //In ith operation: set  s[idx[i]] = ci


    //Find the smallest lexicographical string after transformation

    int n = idx.size();
    
    for(int i 0;i<)

}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin>>t;
    while(t--)
    {
        int n,m;
        cin>>n>>m;

        string s;
        cin>>s;
        
        vector<int>idx(m);
        for(auto&x:idx)cin>>x;


        string c;
        cin>>c;

        cout<<solve()<<endl;

        
    }
    return 0;
}
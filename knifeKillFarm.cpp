#include<bits/stdc++.h>
#define ll long long
using namespace std;

      
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin>>t;

    while(t--)
    {
        int n,m;
        cin>>n>>m;

        vector<int>a(n);
        for(auto&x:a)cin>>x;

        /*

        He must publish exaltly m drafts in their original order
         
        When he publishes the ith chosen draft, his score changes by i *(bi- bi-1) and b0 = 0


        Determine the max score he can achieve by choosing whichever drafts he wants
        
        
        */

        int ans=0;
        
    
    }

    return 0;
}
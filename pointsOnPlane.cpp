#include <bits/stdc++.h>
using namespace std;

int solve(int n)
{
    /*

    We have to place n chips

    Cost of placing chip at x,y = x+y

    */
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;

        cout << solve(n) << endl;
    }
    return 0;
}
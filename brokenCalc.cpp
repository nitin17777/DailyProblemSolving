#include <bits/stdc++.h>
using namespace std;

int brokenCalc(int startValue, int target)
{
    // Return the min number of operations needed to display target

    // In one operation we can: Mulitply the number displayed by 2 or subtract 1 from it

    int cnt = 0;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    cout << brokenCalc(2, 3) << endl;
    return 0;
}
#include <bits/stdc++.h>
using namespace std;

int numRabbits(vector<int> &answers)
{
    int n = nums.size();

    /*

    We asked rabbitshow many have the same colors as you
    and got ans[i] from every rabbit

    Return the min number of rabbits that could be in the forest

    */
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    vector<int> ans = {1, 1, 2};
    cout << numRabbits(ans) << endl;

    return 0;
}
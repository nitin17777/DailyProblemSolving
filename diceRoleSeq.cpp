#include <bits/stdc++.h>
using namespace std;

int solve(vector<int> &a)
{

    int n = a.size();

    // Return the min number of ops to make
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;

        for (auto &x : a)
            cin >> x;
        cout << solve(a) << endl;
    }
    return 0;
}
#include<bits/stdc++.h>
#define ll long long
using namespace std;

      
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin>>t;

    while(t--)
    {
        int n;
        cin>>n;

        vector<int>a(n);
        for(auto&x:a)cin>>x;

        //Choose k distincts array indices and calculate x = AND of all these elements 
        //Select x from each choosen element 

        //Values of k such that it's possible to make all elements of array equal to 0 


    
    }

    return 0;
}
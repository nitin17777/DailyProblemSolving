#include<iostream>
#include<vector>

using namespace std;

int maxNum(int n,int m, vector<int>&a,vector<int>&b)
{
    //John takes one min tot run to other side of the gym

    //If he chooses to run to the other side of the gym he gains one point 
    

    //Fj will run the test until the start of mth minute
    //initially he is at the starting side of the gym which we will denote as side 0 and opposite side is denoted side 1

    //the audio plays n times

    //at start of a ith minute FJ must be at the b ith side of the gym
    //max number of points FJ can acquire while ensuring that he meets the audio's requirements


    

}

int main()
{
    vector<int>a = {2,1};
    vector<int>b = {4,0};

    cout<<maxNum(n,m,a,b)<<endl;
    return 0;
    
}

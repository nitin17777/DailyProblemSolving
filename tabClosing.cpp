#include <bits/stdc++.h>
using namespace std;

int mini(int a, int b, int n)
{
    /*
    We need to find the minimum number of times,we need to move the mouse to close all the tabs

    Tabs are such at : len , 2*len,...

    a -> length of screen
    b ->
    n->number of tabs

    */
}

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        int a, b, n;
        cin >> a >> b >> n;

        cout << mini(a, b, n) << endl;
    }
    return 0;
}

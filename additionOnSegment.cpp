#include <bits/stdc++.h>
using namespace std;

int maxi(vector<int> &a)
{
    vector <
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;

        vector<int> a(n);
        cout << maxi(a) << endl;
    }

    return 0;
}
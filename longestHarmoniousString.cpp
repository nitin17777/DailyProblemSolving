#include<iostream>
#include<vector>

using namespace std;


int findLHS(vector<int>&numms)
{
    //We define a harmonious array as an array where the difference between its maximum value and its minimum value is exactly 1
    //we have to return the length of this longest Harmonius Subsequence

    int maxLength = 0;

    for(int i = i; i <nums.size(); i++)
    {
        //we have to find the length of maximum subsequence that is made from nums[i] and nums[i]+1
        int findNum = nums[i];

        
        if( )
    }



}



int main()
{
    vector<int>nums = {1,3,2,2,5,2,3,7};

    int ans = findLHS(nums);

    cout<<ans<<endl;

    return 0;
    
}

#include <iostream>
#include <vector>

using namespace std;

int minDistance(string word1, string word2)
{
    // we have to return the minimum number of operations to convert word1 into word2

    // we can only insert, delete or replace a character at a time

    int m = word1.size(), n = word2.size();
}

int main()
{

    cout << minDistance("horse", "ros") << endl;

    return 0;
}

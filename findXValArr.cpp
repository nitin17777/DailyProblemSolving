#include<bits/stdc++.h>
#define ll long long
using namespace std;

vector<ll> resultArray(vector<int>& nums, int k)
{
    vector<ll>ans;
    int n = a.size();

    //In one operation : We can remove any overlapping  prefix in nums such that nums remains non empty

    //Find the x value of nums -> 

    //And return an array result of size k where result[x] = x value of nums : 0<=x<=k-1

    // x value of nums ...products of remaining elements leave remainder x when divided by k
    


}
      
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

   
    vector<int>nums = {1,2,3,4,5};
    cout<<resultArray(nums)<<endl;

    return 0;
}
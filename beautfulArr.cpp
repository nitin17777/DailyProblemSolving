#include <bits/stdc++.h>
using namespace std;

int countArrangement(int n)
{

    /*
    Beautiful arrangement is such that: perm[i] %i == 0
                                        i % perm[i] == 0
    */

    // Return the number of beautiful arrangements we can construct
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    cout << countArrangement(2) << endl;
    return 0;
}
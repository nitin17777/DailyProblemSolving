#include<bits/stdc++.h>
using namespace std;

int solve(vector<int>&arr)
{

    //cost of the permutation = min length of contiguous subsegment that needs to be sorted so that entire population becomes sorted in ascending order


    //we have array consisting of integers from 0 to n where no integer appears more than once
    //replce zeroes with integers so that array becomes permutation
    

    //

    //calculate the max possible cost of resulting permutation






}

int main()
{

    vector<int>arr = {1,0,4,0,5};
    cout<<solve(arr)<<endl;

    return 0;
    
}

#include <bits/stdc++.h>
using namespace std;

int solve(vector<int> &arr)
{

    // cost = min length of continuous subsegment that need to be sorted such that entire permutation becomes sorted

    // Replace zeroes with integers so that array p becomes a premutation
    // calculate  max possible cost
}

int main()
{

    vector<int> arr = {1, 0, 4, 0, 5};
    cout << solve(arr) << endl;

    return 0;
}

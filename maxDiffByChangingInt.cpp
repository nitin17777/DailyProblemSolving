#include<isotream>
using namespace std;

    int maxDiff(int num)
    {
        //pick any integer and repalce it with any integer and do it again to find another number in order to find the max difference between these two newly formed numbers
        string s = to_string(num);
        


    }

int main()
{

    int num = 555;
    int ans = maxDiff(num);

    cout<<ans<<endl;
    return 0;

}
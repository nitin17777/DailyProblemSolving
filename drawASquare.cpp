#include <bits/stdc++.h>
using namespace std;
#define ll long long

bool is(ll l, ll r, ll d, ll u)
{
    // lu ur rd rl
    //  root(l^2 + u^2)
}

ll main()
{

    ll t;
    ;
    cin >> t;

    while (t--)
    {

        ll l, r, d, u;
        cin >> l >> r >> d >> u;

        cout << is(l, r, d, u) << "\n";
    }
    return 0;
}

#include<bits/stdc++.h>
#define ll long long
using namespace std;

      
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin>>t;

    while(t--)
    {
        int n,d;
        cin>>n>>d;

        for(auto & x:a)cin>>x;

        //field view for a person = d people clockwise and d people anticlockwise

        //IF person i recieves a gift, x people in field who didn't received a gift -> happiness gain = x*ai
        // and -x*ai happiness if i doesn't recieve a gift
        
        //Maximise total happiness of n people combined

        
    
    }

    return 0;
}
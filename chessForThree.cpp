#include <iostream>
#include <vector>

using namespace std;

int scores(vector<int> &scores)
{
    // winner ->2 , draw ->1

    // scores after all games are given
    // Find the max number of draws that could have happened
}

int main()
{
    vector<int> scores = {0, 1, 1};
    cout << draws(scores) << endl;
    return 0;
}

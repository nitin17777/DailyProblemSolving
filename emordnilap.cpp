#include <bits/stdc++.h>
using namespace std;

int find(int n)
{
    /*

    Given a permutation p of n numebrs

    We create an array a of 2n numbers which is equal to to p joined with it's reverse


    */
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;

        cout << find(n) << endl;
    }
    return 0;
}
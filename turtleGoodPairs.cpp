#include<bits/stdc++.h>
#define ll long long
using namespace std;

      
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin>>t;

    while(t--)
    {
        int n;
        cin>>n;

        string s;
        cin>>s;

        //Pleasant pair iff: i<= k <= j and sk != sk+1 ||| sk != si or sk+1 != sj

        //Good Pair iff: i<j : si != sj or (i,j) is a pleasant pair

        //we want to maximise the number of good pairs: Find such string
         




    
    }

    return 0;
}
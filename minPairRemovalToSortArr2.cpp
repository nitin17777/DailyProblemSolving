#include <bits/stdc++.h>
using namespace std;

int minimumPairRemoval(vector<int> &nums)
{
    /*
    Select adj pairs with min sum in nums
    Replace the pair with their sum
    Return the min number of ops to make the array non decreasing

    */
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    vector<int> nums = {5, 2, 3, 1};
    cout << minimumPairRemoval(nums) << endl;

    return 0;
}
#include <bits/stdc++.h>
using namespace std;

int solve(int l, int r)
{

    // Return the max number of operations we can perform
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--)
    {
        int l, r;
        cin >> l >> r;

        cout << solve(l, r) << endl;
    }
    return 0;
}
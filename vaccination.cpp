#include <bits/stdc++.h>
using namespace std;

int solve(int)
{
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--)
    {
        int n, k, d, w;
        cin >> n >> k >> d >> w;

        vector<int> a(n);
        for (auto &x : a)
            cin >> x;

        cout << solve() << endl;
    }
    return 0;
}
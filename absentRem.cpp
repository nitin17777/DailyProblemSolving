#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;

        vector<int> a(n);
        for (auto &x : a)
            cin >> x;

        /*Find n/2 different pairs of integers x and y such that:
        x !=y

        x and y appear in a
        x mod y doesnt appear in a

        */
    }
    return 0;
}
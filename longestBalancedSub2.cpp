#include <bits/stdc++.h>
using namespace std;

int longestBalanced(string s)
{
    int n = s.size();
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    cout << longestBalanced("abbac") << endl;

    return 0;
}